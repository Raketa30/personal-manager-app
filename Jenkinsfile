pipeline {

    agent any

    stages {
        stage('build') {
            steps {
                echo 'build'
                gradle build
            }
        }

        stage('test') {

        }

        stage('push to docker hub') {

        }

        stage('pull by ssh') {

        }
    }
}